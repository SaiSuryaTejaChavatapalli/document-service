const { exec } = require('child_process');
// create async function to scan file assign it scanFile constant 
const scanFile = async (req, res, next) => {
    try {

    } catch (error) {
        
    }
};

module.exports = scanFile;